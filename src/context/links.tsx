// interface Props {
//   children?: React.ReactNode;
// }

// import { createContext } from "react";

// export const LinksContext = createContext(null);

// export const linksProvider = ({children}: Props) => {
//   return <LinksContext.Provider value={null}>{children}</LinksContext.Provider>;
// };
